// Package xdatly is the curated Datly 1.0 public contract workspace.
//
// It owns public, implementation-agnostic primitives only. If another runtime
// could reasonably implement the contract, it may belong here; if it depends
// on Datly's internal execution model, routing, SQL runtime, materialization,
// or transport glue, it does not.
package xdatly
